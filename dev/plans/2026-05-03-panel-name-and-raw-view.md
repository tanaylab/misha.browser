# Panel name strip label & raw view — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the panel `name` visible as a left-side strip label on data panels, and add a "raw" rendering mode (no smoothing, NAs as gaps) toggleable from API, YAML, or Shiny UI.

**Architecture:** Two independent features in one PR. Feature 1 uses a synthetic `facet_grid` with `switch = "y"` so the panel name renders as a ggplot strip without restructuring `browser_plot()`'s patchwork composition. Feature 2 short-circuits the dynamic-mode and smoothing branches in `extract_panel_data()` when an `effective_raw` flag is set, with the flag resolved from a `state > panel > cfg` precedence chain.

**Tech Stack:** R, ggplot2 (>= 3.4.0), patchwork, shiny, testthat.

**Spec:** `dev/specs/2026-05-03-panel-name-and-raw-view-design.md`

**Note about cache_key:** `R/cache.R:54` defines `cache_key <- function(...) digest::digest(list(...))`. It is variadic, so adding `effective_raw` to the call site at `R/extract.R:137-145` is sufficient to differentiate cache keys — no signature change needed.

---

## File Structure

Files this plan touches:

- `R/browser.R` — add `show_name` and `raw` arguments to `browser_add_panel()`.
- `R/config.R` — `validate_panel()` accepts `show_name` (defaulted to `TRUE`) and `raw` (passed through with no default so `NULL` means "inherit"). Add `raw` to `panel$._cache_signature`.
- `R/panels.R` — synthetic facet for panel name strip in `render_data_panel()`; strip styling in `apply_panel_theme()`.
- `R/extract.R` — compute `effective_raw` at top of `extract_panel_data()`; force `extraction_mode = "fixed"`, drop dynamic-mode iterator/vtrack-shift logic, drop rollmean smoothing, filter `smooth` transforms; pass `effective_raw` as an extra positional arg to `cache_key()`.
- `R/shiny-ui.R` — add `checkboxInput("raw_view")` next to the smooth slider in `browser_ui()`.
- `R/shiny-server.R` — observer for `input$raw_view` updating `br$state$raw_view`.
- `tests/testthat/test-config.R` — add tests for `show_name` default, `raw` pass-through, and cache-signature inclusion.
- `tests/testthat/test-browser.R` — add tests for `browser_add_panel()` accepting `show_name` and `raw`.
- `tests/testthat/test-panels.R` — add tests for synthetic-facet inclusion / suppression.
- `tests/testthat/test-extract.R` — add tests for raw precedence and behavior.
- `tests/testthat/test-shiny-ui.R` — add a test that `browser_ui()` HTML contains `raw_view`.
- `man/browser_add_panel.Rd` — regenerated by roxygen.

---

## Task 1: `validate_panel()` accepts `show_name` (default `TRUE`)

**Files:**
- Modify: `R/config.R:182-260`
- Test: `tests/testthat/test-config.R`

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test-config.R`:

```r
test_that("validate_panel defaults show_name to TRUE for data panels", {
    panel <- list(name = "sig", tracks = c("track1"))
    result <- validate_panel(panel, 1)
    expect_true(result$show_name)
})

test_that("validate_panel preserves show_name = FALSE", {
    panel <- list(name = "sig", tracks = c("track1"), show_name = FALSE)
    result <- validate_panel(panel, 1)
    expect_false(result$show_name)
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test(filter = "config")'
```

Expected: the two new tests FAIL because `result$show_name` is NULL.

- [ ] **Step 3: Implement minimal change in `validate_panel()`**

In `R/config.R`, inside the `if (panel$type == "data") { ... }` block, immediately after the existing `panel$show_legend <- panel$show_legend %||% TRUE` line (around line 214), add:

```r
        panel$show_name <- panel$show_name %||% TRUE
```

- [ ] **Step 4: Run tests**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test(filter = "config")'
```

Expected: all config tests PASS.

- [ ] **Step 5: Commit**

```bash
cd /home/aviezerl/src/misha.browser && git add R/config.R tests/testthat/test-config.R && git commit -m "Add show_name panel option (default TRUE)"
```

---

## Task 2: `validate_panel()` passes through `raw` and includes it in cache signature

**Files:**
- Modify: `R/config.R:248-257`
- Test: `tests/testthat/test-config.R`

- [ ] **Step 1: Write the failing tests**

Append to `tests/testthat/test-config.R`:

```r
test_that("validate_panel leaves raw NULL when unset (so global can take over)", {
    panel <- list(name = "sig", tracks = c("track1"))
    result <- validate_panel(panel, 1)
    expect_null(result$raw)
})

test_that("validate_panel preserves explicit raw = TRUE", {
    panel <- list(name = "sig", tracks = c("track1"), raw = TRUE)
    result <- validate_panel(panel, 1)
    expect_true(result$raw)
})

test_that("validate_panel preserves explicit raw = FALSE", {
    panel <- list(name = "sig", tracks = c("track1"), raw = FALSE)
    result <- validate_panel(panel, 1)
    expect_false(result$raw)
})

test_that("validate_panel includes raw in cache signature", {
    p1 <- validate_panel(list(name = "sig", tracks = c("t1")), 1)
    p2 <- validate_panel(list(name = "sig", tracks = c("t1"), raw = TRUE), 1)
    expect_false(identical(p1$._cache_signature, p2$._cache_signature))
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test(filter = "config")'
```

Expected: the cache-signature test FAILS because `raw` isn't in the digest. The other three pass since `raw` is just propagated through `panel`.

- [ ] **Step 3: Implement**

In `R/config.R`, find the cache-signature block (lines 248-257):

```r
    if (panel$type == "data") {
        panel$._cache_signature <- digest::digest(list(
            transforms = panel$transforms,
            grouping = panel$grouping,
            facet_by = panel$facet_by,
            plot_type = panel$plot_type,
            smooth_window = panel$smooth_window
        ))
    }
```

Replace with:

```r
    if (panel$type == "data") {
        panel$._cache_signature <- digest::digest(list(
            transforms = panel$transforms,
            grouping = panel$grouping,
            facet_by = panel$facet_by,
            plot_type = panel$plot_type,
            smooth_window = panel$smooth_window,
            raw = panel$raw
        ))
    }
```

(Do NOT add `panel$raw <- panel$raw %||% FALSE` — leaving NULL when unset is intentional, see spec section "Precedence".)

- [ ] **Step 4: Run tests**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test(filter = "config")'
```

Expected: all config tests PASS.

- [ ] **Step 5: Commit**

```bash
cd /home/aviezerl/src/misha.browser && git add R/config.R tests/testthat/test-config.R && git commit -m "Add raw panel option pass-through and cache-signature inclusion"
```

---

## Task 3: `browser_add_panel()` exposes `show_name` and `raw`

**Files:**
- Modify: `R/browser.R:401-474`
- Test: `tests/testthat/test-browser.R` (append)

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test-browser.R`:

```r
test_that("browser_add_panel accepts show_name argument", {
    br <- browser_create() |>
        browser_add_panel(name = "sig", tracks = c("t1"), show_name = FALSE)
    expect_false(br$cfg$panels[[1]]$show_name)
})

test_that("browser_add_panel accepts raw argument", {
    br <- browser_create() |>
        browser_add_panel(name = "sig", tracks = c("t1"), raw = TRUE)
    expect_true(br$cfg$panels[[1]]$raw)
})

test_that("browser_add_panel leaves raw NULL by default", {
    br <- browser_create() |>
        browser_add_panel(name = "sig", tracks = c("t1"))
    expect_null(br$cfg$panels[[1]]$raw)
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test(filter = "browser")'
```

Expected: the show_name and raw tests FAIL.

- [ ] **Step 3: Implement**

In `R/browser.R`, replace `browser_add_panel()` (lines 416-474) with:

```r
browser_add_panel <- function(browser, name, tracks = NULL,
                              grouping = NULL,
                              facet_by = NULL,
                              transforms = list(),
                              plot_type = "line",
                              colors = NULL,
                              ylim = NULL,
                              height = 2,
                              show_name = TRUE,
                              raw = NULL,
                              ...) {
    # Auto-create vtracks for each track
    if (!is.null(tracks) && length(tracks) > 0) {
        existing_vtrack_names <- vapply(
            browser$cfg$vtracks %||% list(),
            function(v) v$name,
            character(1)
        )
        for (track in tracks) {
            if (!is.character(track) || length(track) != 1 || is.na(track) || !nzchar(track)) {
                next
            }
            if (track %in% existing_vtrack_names) {
                next
            }
            vtrack <- list(
                name = track,
                src = track,
                func = "avg"
            )
            browser$cfg$vtracks <- c(browser$cfg$vtracks %||% list(), list(vtrack))
            existing_vtrack_names <- c(existing_vtrack_names, track)
        }
    }

    panel <- list(
        name = name,
        type = "data",
        tracks = tracks,
        grouping = grouping,
        facet_by = facet_by,
        transforms = transforms,
        plot_type = plot_type,
        colors = colors,
        ylim = ylim,
        height = height,
        show_name = show_name,
        raw = raw
    )

    extra <- list(...)
    for (opt in names(extra)) {
        panel[[opt]] <- extra[[opt]]
    }

    panel <- validate_panel(panel, length(browser$cfg$panels) + 1)
    browser$cfg$panels <- c(browser$cfg$panels, list(panel))

    browser
}
```

Update the roxygen block above the function. Insert these two `@param` lines just before the existing `@param ...` line (currently at 413):

```r
#' @param show_name Logical, render the panel name as a left-side strip label (default TRUE)
#' @param raw Logical or NULL. If TRUE, render without smoothing and with NAs as gaps. NULL inherits from cfg$plot$raw.
```

- [ ] **Step 4: Regenerate roxygen docs**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::document()'
```

Expected: `man/browser_add_panel.Rd` updated; no errors.

- [ ] **Step 5: Run tests**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test(filter = "browser")'
```

Expected: all browser tests PASS.

- [ ] **Step 6: Commit**

```bash
cd /home/aviezerl/src/misha.browser && git add R/browser.R tests/testthat/test-browser.R man/browser_add_panel.Rd && git commit -m "Expose show_name and raw on browser_add_panel()"
```

---

## Task 4: Synthetic facet for panel name strip

**Files:**
- Modify: `R/panels.R:14-89` (`render_data_panel`)
- Test: `tests/testthat/test-panels.R`

- [ ] **Step 1: Write the failing tests**

Append to `tests/testthat/test-panels.R`:

```r
# =============================================================================
# Tests for panel-name strip (Feature 1)
# =============================================================================

# Helper: minimal pre-extracted data so render_data_panel skips extraction
.panel_strip_data <- function() {
    data.frame(
        chrom = "chr1",
        start = seq(1, 1000, by = 100),
        end   = seq(100, 1099, by = 100),
        pos   = seq(50, 1049, by = 100),
        value = runif(10),
        track = rep("t1", 10),
        source = rep("src", 10),
        stringsAsFactors = FALSE
    )
}

test_that("data panel injects ._panel_name and uses FacetGrid when show_name = TRUE", {
    panel <- list(
        name = "my_signal",
        type = "data",
        tracks = "t1",
        plot_type = "line",
        show_name = TRUE
    )
    panel$._cfg_colors <- list()
    region <- data.frame(chrom = "chr1", start = 1, end = 1100)

    p <- render_data_panel(
        browser = list(cfg = list(colors = list())),
        panel = panel,
        region = region,
        vlines_data = NULL,
        pre_extracted_data = .panel_strip_data()
    )

    expect_true("._panel_name" %in% names(p$data))
    expect_equal(unique(p$data$._panel_name), "my_signal")
    expect_s3_class(p$facet, "FacetGrid")
})

test_that("data panel does NOT add ._panel_name when show_name = FALSE", {
    panel <- list(
        name = "my_signal",
        type = "data",
        tracks = "t1",
        plot_type = "line",
        show_name = FALSE
    )
    panel$._cfg_colors <- list()
    region <- data.frame(chrom = "chr1", start = 1, end = 1100)

    p <- render_data_panel(
        browser = list(cfg = list(colors = list())),
        panel = panel,
        region = region,
        vlines_data = NULL,
        pre_extracted_data = .panel_strip_data()
    )

    expect_false("._panel_name" %in% names(p$data))
    expect_s3_class(p$facet, "FacetNull")
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test(filter = "panels")'
```

Expected: FAIL — no `._panel_name` column, facet is `FacetNull`.

- [ ] **Step 3: Implement in `render_data_panel()`**

In `R/panels.R`, locate the section between data extraction and base plot construction (around lines 44-56):

```r
    x_limits <- c(region$start, region$end)

    # Get color mapping
    color_by <- panel$grouping$color_by %||% "track"
    colors <- get_panel_colors(panel, data, color_by, browser$cfg$colors)

    # Base plot with data
    p <- ggplot2::ggplot(data, ggplot2::aes(x = pos, y = value))
```

Replace with:

```r
    x_limits <- c(region$start, region$end)

    # Get color mapping
    color_by <- panel$grouping$color_by %||% "track"
    colors <- get_panel_colors(panel, data, color_by, browser$cfg$colors)

    # Inject panel-name column for synthetic facet (Feature 1)
    show_name <- isTRUE(panel$show_name %||% TRUE)
    if (show_name && !is.null(panel$name) && nzchar(panel$name)) {
        data[["._panel_name"]] <- panel$name
    } else {
        show_name <- FALSE
    }

    # Base plot with data
    p <- ggplot2::ggplot(data, ggplot2::aes(x = pos, y = value))
```

Then find the existing facet block (around lines 60-65):

```r
    # Apply faceting if specified
    if (!is.null(panel$facet_by) && panel$facet_by %in% names(data)) {
        p <- p + ggplot2::facet_grid(
            stats::as.formula(paste(panel$facet_by, "~ .")),
            scales = "free_y"
        )
    }
```

Replace with:

```r
    # Apply faceting (Feature 1 panel-name strip + optional facet_by)
    has_facet_by <- !is.null(panel$facet_by) && panel$facet_by %in% names(data)
    has_panel_strip <- show_name && "._panel_name" %in% names(data)

    if (has_facet_by && has_panel_strip) {
        p <- p + ggplot2::facet_grid(
            stats::as.formula(paste(panel$facet_by, "~ ._panel_name")),
            scales = "free_y", switch = "y"
        )
    } else if (has_facet_by) {
        p <- p + ggplot2::facet_grid(
            stats::as.formula(paste(panel$facet_by, "~ .")),
            scales = "free_y"
        )
    } else if (has_panel_strip) {
        p <- p + ggplot2::facet_grid(
            ._panel_name ~ .,
            switch = "y"
        )
    }
```

- [ ] **Step 4: Run tests**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test(filter = "panels")'
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
cd /home/aviezerl/src/misha.browser && git add R/panels.R tests/testthat/test-panels.R && git commit -m "Render panel name as left-side strip via synthetic facet"
```

---

## Task 5: Strip styling in `apply_panel_theme()`

**Files:**
- Modify: `R/panels.R:263-284`

> No structural unit test for this task — theme element introspection is brittle under ggplot2 4.0's S7 migration. We rely on the visual smoke check below and the integration smoke test in Task 10.

- [ ] **Step 1: Implement**

In `R/panels.R`, replace `apply_panel_theme()` (lines 263-284):

```r
apply_panel_theme <- function(p, panel) {
    p <- p + ggplot2::theme_bw()

    legend_pos <- if (panel$show_legend %||% TRUE) "bottom" else "none"
    y_title <- panel$y_title %||% ""

    p <- p + ggplot2::theme(
        legend.position = legend_pos,
        strip.background = ggplot2::element_rect(fill = "grey95"),
        strip.background.y = ggplot2::element_rect(fill = "grey90", color = NA),
        strip.text = ggplot2::element_text(size = 11, face = "bold"),
        strip.text.y.left = ggplot2::element_text(angle = 0, face = "bold", hjust = 1),
        strip.placement = "outside",
        panel.grid.minor = ggplot2::element_blank(),
        axis.title.x = ggplot2::element_blank(),
        axis.text.x = ggplot2::element_blank(),
        axis.ticks.x = ggplot2::element_blank(),
        plot.margin = ggplot2::margin(b = 2, t = 2)
    ) + ggplot2::labs(y = y_title, color = NULL)

    p
}
```

- [ ] **Step 2: Visual smoke check**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e '
devtools::load_all()
d <- data.frame(pos = 1:100, value = sin(seq(0, 6, length.out = 100)),
                track = "t1", source = "s", chrom = "chr1",
                start = 1:100, end = 1:100)
panel <- list(name = "demo_panel", type = "data", tracks = "t1",
              plot_type = "line", show_name = TRUE,
              y_title = "signal", show_legend = TRUE)
panel$._cfg_colors <- list()
region <- data.frame(chrom = "chr1", start = 1, end = 100)
p <- render_data_panel(list(cfg = list(colors = list())), panel, region, NULL, pre_extracted_data = d)
ggplot2::ggsave("/tmp/panel_strip_check.png", p, width = 8, height = 2.5, dpi = 100)
cat("Saved /tmp/panel_strip_check.png\n")'
```

Open `/tmp/panel_strip_check.png` and confirm: gray vertical strip on the left of the plot panel labelled "demo_panel" in bold, horizontal text; y-axis title "signal" to the left of the strip; line plot to the right.

- [ ] **Step 3: Run all panel tests**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test(filter = "panels")'
```

Expected: PASS.

- [ ] **Step 4: Commit**

```bash
cd /home/aviezerl/src/misha.browser && git add R/panels.R && git commit -m "Style panel-name strip as horizontal bold left label"
```

---

## Task 6: `extract_panel_data()` honors `effective_raw`

**Files:**
- Modify: `R/extract.R:23-220` (`extract_panel_data`)
- Test: `tests/testthat/test-extract.R`

- [ ] **Step 1: Write the failing tests**

Append to `tests/testthat/test-extract.R`:

```r
# =============================================================================
# Tests for raw view (Feature 2)
# =============================================================================

test_that("effective_raw resolves from state > panel > cfg > FALSE", {
    # Mirror the precedence chain extract_panel_data computes.
    resolve <- function(state, panel, cfg) {
        isTRUE(state %||% panel %||% cfg %||% FALSE)
    }
    expect_false(resolve(NULL, NULL, NULL))
    expect_true(resolve(NULL, NULL, TRUE))     # cfg only
    expect_false(resolve(NULL, FALSE, TRUE))    # panel overrides cfg
    expect_true(resolve(NULL, TRUE, FALSE))     # panel overrides cfg
    expect_true(resolve(TRUE, FALSE, FALSE))    # state overrides panel
    expect_false(resolve(FALSE, TRUE, TRUE))    # state FALSE overrides
})

test_that("extract_panel_data uses base iterator under raw (no dynamic adjust)", {
    # Stub all the package internals so we can run extract_panel_data without
    # a misha database. We capture the iterator passed to extract_tracks.
    captured <- new.env()

    fake_extract <- function(tracks, region, iterator, colnames) {
        captured$iterator <- iterator
        df <- data.frame(
            chrom = "chr1",
            start = 1:5, end = 1:5, pos = 1:5,
            stringsAsFactors = FALSE
        )
        df[[colnames[[1]]]] <- c(NA_real_, 2, NA_real_, 4, NA_real_)
        df
    }

    testthat::local_mocked_bindings(
        extract_tracks = fake_extract,
        resolve_track_specs = function(...) list(
            exprs = "t1", names = "t1", temp_vtracks = character(0)
        ),
        cleanup_temp_vtracks = function(...) invisible(NULL),
        is_vtrack = function(...) FALSE,
        cache_exists = function(...) FALSE,
        cache_get = function(...) NULL,
        cache_set = function(...) invisible(NULL),
        add_track_metadata = function(data, panel, track_names) data,
        .package = "misha.browser"
    )

    cfg <- list(
        plot = list(extraction_mode = "dynamic_smooth", iterator = 32, target_points = 4000),
        vtracks = list()
    )
    br <- list(cfg = cfg, state = list(smooth_window = 100, raw_view = TRUE))
    panel <- list(
        name = "p",
        type = "data",
        tracks = c("t1"),
        transforms = list(list(type = "smooth", window = 50))
    )
    region <- data.frame(chrom = "chr1", start = 1, end = 1000)

    out <- extract_panel_data(br, panel, region, use_cache = FALSE)

    # Base iterator (32) should be used because raw forces fixed mode and
    # skips the dynamic-mode iterator recalculation.
    expect_equal(captured$iterator, 32)
    # Smoothing should be skipped, so NAs from extraction stay as NAs.
    expect_true(any(is.na(out$t1)))
})

test_that("extract_panel_data smooths normally when raw is OFF", {
    fake_extract <- function(tracks, region, iterator, colnames) {
        df <- data.frame(
            chrom = "chr1",
            start = seq_len(20), end = seq_len(20), pos = seq_len(20),
            stringsAsFactors = FALSE
        )
        df[[colnames[[1]]]] <- as.numeric(seq_len(20))
        df
    }

    testthat::local_mocked_bindings(
        extract_tracks = fake_extract,
        resolve_track_specs = function(...) list(
            exprs = "t1", names = "t1", temp_vtracks = character(0)
        ),
        cleanup_temp_vtracks = function(...) invisible(NULL),
        is_vtrack = function(...) FALSE,
        cache_exists = function(...) FALSE,
        cache_get = function(...) NULL,
        cache_set = function(...) invisible(NULL),
        add_track_metadata = function(data, panel, track_names) data,
        .package = "misha.browser"
    )

    cfg <- list(plot = list(extraction_mode = "fixed", iterator = 1), vtracks = list())
    br <- list(cfg = cfg, state = list(smooth_window = 5, raw_view = FALSE))
    panel <- list(name = "p", type = "data", tracks = c("t1"), transforms = list())
    region <- data.frame(chrom = "chr1", start = 1, end = 1000)

    out <- extract_panel_data(br, panel, region, use_cache = FALSE)

    # With smoothing on (window 5), the first value should be a rolling mean,
    # not the raw 1.0 from the input.
    expect_false(out$t1[3] == 3.0)
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test(filter = "extract")'
```

Expected: the precedence test PASSES (pure local logic). The "uses base iterator under raw" test FAILS because dynamic_smooth currently overrides the iterator. The "smooths normally when raw is OFF" test should pass already.

- [ ] **Step 3: Implement raw resolution at top of `extract_panel_data()`**

In `R/extract.R`, after the `region <- sanitize_interval(region)` block (line 34), insert:

```r
    # Resolve raw view (state > panel > cfg > FALSE)
    effective_raw <- isTRUE(
        browser$state$raw_view %||%
            panel$raw %||%
            browser$cfg$plot$raw %||%
            FALSE
    )
```

- [ ] **Step 4: Force fixed mode under raw**

Change the line at 40 from:

```r
    extraction_mode <- browser$cfg$plot$extraction_mode %||% "fixed"
```

To:

```r
    extraction_mode <- if (effective_raw) "fixed" else (browser$cfg$plot$extraction_mode %||% "fixed")
```

The existing `if/else if/else` at lines 47-85 already routes to the "fixed mode: use base iterator" branch when `extraction_mode == "fixed"`, so under raw it now does the right thing without further changes there.

- [ ] **Step 5: Skip rollmean smoothing under raw**

Replace the smoothing block at lines 174-189:

```r
    transforms <- panel$transforms %||% list()
    smooth_window <- panel$smooth_window %||% browser$state$smooth_window
    if (extraction_mode %in% c("dynamic", "dynamic_smooth")) {
        # In dynamic/dynamic_smooth modes, skip smooth transforms
        # (iterator or vtrack aggregation provides smoothing)
        transforms <- Filter(function(t) t$type != "smooth", transforms)
    } else if (!is.null(smooth_window)) {
        # In fixed mode, apply smooth window from state
        has_smooth <- any(vapply(transforms, function(t) identical(t$type, "smooth"), logical(1)))
        if (has_smooth) {
            transforms <- update_smooth_window(transforms, smooth_window)
        } else if (smooth_window > 1) {
            transforms <- c(transforms, list(list(type = "smooth", window = smooth_window)))
        }
    }
```

With:

```r
    transforms <- panel$transforms %||% list()
    smooth_window <- panel$smooth_window %||% browser$state$smooth_window
    if (effective_raw) {
        # Raw view: drop all smoothing entirely
        transforms <- Filter(function(t) t$type != "smooth", transforms)
    } else if (extraction_mode %in% c("dynamic", "dynamic_smooth")) {
        # In dynamic/dynamic_smooth modes, skip smooth transforms
        # (iterator or vtrack aggregation provides smoothing)
        transforms <- Filter(function(t) t$type != "smooth", transforms)
    } else if (!is.null(smooth_window)) {
        # In fixed mode, apply smooth window from state
        has_smooth <- any(vapply(transforms, function(t) identical(t$type, "smooth"), logical(1)))
        if (has_smooth) {
            transforms <- update_smooth_window(transforms, smooth_window)
        } else if (smooth_window > 1) {
            transforms <- c(transforms, list(list(type = "smooth", window = smooth_window)))
        }
    }
```

- [ ] **Step 6: Skip per-vtrack smooth transforms under raw**

Replace the per-vtrack smoothing block at lines 196-209:

```r
            vt_transforms <- vt_transforms %||% list()
            if (extraction_mode %in% c("dynamic", "dynamic_smooth")) {
                vt_transforms <- Filter(function(t) t$type != "smooth", vt_transforms)
            } else if (!is.null(browser$state$smooth_window)) {
                vt_transforms <- update_smooth_window(vt_transforms, browser$state$smooth_window)
            }
            data <- apply_transforms(data, vt_transforms, track_name)
```

With:

```r
            vt_transforms <- vt_transforms %||% list()
            if (effective_raw) {
                vt_transforms <- Filter(function(t) t$type != "smooth", vt_transforms)
            } else if (extraction_mode %in% c("dynamic", "dynamic_smooth")) {
                vt_transforms <- Filter(function(t) t$type != "smooth", vt_transforms)
            } else if (!is.null(browser$state$smooth_window)) {
                vt_transforms <- update_smooth_window(vt_transforms, browser$state$smooth_window)
            }
            data <- apply_transforms(data, vt_transforms, track_name)
```

- [ ] **Step 7: Add `effective_raw` to `cache_key()` call**

In `R/extract.R`, find the cache_key call at lines 137-145:

```r
    cache_k <- cache_key(
        panel$tracks,
        panel_signature,
        vtrack_signature,
        region$chrom, region$start, region$end,
        iterator,
        extraction_mode,
        smoothing_key
    )
```

Replace with:

```r
    cache_k <- cache_key(
        panel$tracks,
        panel_signature,
        vtrack_signature,
        region$chrom, region$start, region$end,
        iterator,
        extraction_mode,
        smoothing_key,
        effective_raw
    )
```

- [ ] **Step 8: Run tests**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test(filter = "extract")'
```

Expected: PASS.

- [ ] **Step 9: Commit**

```bash
cd /home/aviezerl/src/misha.browser && git add R/extract.R tests/testthat/test-extract.R && git commit -m "extract_panel_data: honor effective_raw (skip dynamic & smoothing)"
```

---

## Task 7: Shiny UI — `raw_view` checkbox

**Files:**
- Modify: `R/shiny-ui.R:140-167` (the row containing the smooth slider, inside `browser_ui()`)
- Test: `tests/testthat/test-shiny-ui.R`

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test-shiny-ui.R`:

```r
test_that("browser_ui includes raw_view checkbox", {
    br <- browser_create()
    br$cfg$plot$raw <- FALSE
    ui <- browser_ui(br)
    html <- as.character(ui)
    expect_true(grepl('id=\"raw_view\"', html, fixed = TRUE))
})

test_that("browser_ui raw_view default reflects cfg$plot$raw", {
    br <- browser_create()
    br$cfg$plot$raw <- TRUE
    ui <- browser_ui(br)
    html <- as.character(ui)
    # Shiny renders checked checkboxes with checked="checked"
    expect_true(grepl('raw_view.*checked', html))
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test(filter = "shiny-ui")'
```

Expected: FAIL — no `raw_view` element in the HTML.

- [ ] **Step 3: Implement**

In `R/shiny-ui.R`, find the smooth-window column inside `browser_ui()` (lines 157-166):

```r
                # Smooth window
                shiny::column(
                    2,
                    shiny::numericInput(
                        "smooth_window",
                        "Smooth",
                        value = cfg$ui$smooth_window_default %||% 10,
                        min = 1, step = 5, width = "100%"
                    )
                )
```

Replace with:

```r
                # Smooth window + raw toggle
                shiny::column(
                    2,
                    shiny::numericInput(
                        "smooth_window",
                        "Smooth",
                        value = cfg$ui$smooth_window_default %||% 10,
                        min = 1, step = 5, width = "100%"
                    ),
                    shiny::checkboxInput(
                        inputId = "raw_view",
                        label = "Raw (no smoothing, NAs as gaps)",
                        value = isTRUE(cfg$plot$raw)
                    )
                )
```

- [ ] **Step 4: Run tests**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test(filter = "shiny-ui")'
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
cd /home/aviezerl/src/misha.browser && git add R/shiny-ui.R tests/testthat/test-shiny-ui.R && git commit -m "Shiny UI: add raw_view checkbox next to smooth slider"
```

---

## Task 8: Shiny server observer for `raw_view`

**Files:**
- Modify: `R/shiny-server.R` (next to the existing `smooth_window` observer)

- [ ] **Step 1: Locate the existing observer**

```bash
cd /home/aviezerl/src/misha.browser && grep -n "observeEvent(input\\$smooth_window" R/shiny-server.R
```

Read the surrounding 15 lines (`sed -n` is fine here, or use `Read`) to copy the existing observer's indentation and `br` accessor pattern.

- [ ] **Step 2: Implement**

Immediately after the existing `observeEvent(input$smooth_window, { ... })` block (around `R/shiny-server.R:180-189`), insert a new observer using the same indentation and the same `br` reactive accessor as the existing one. The body should be:

```r
        shiny::observeEvent(input$raw_view, {
            br$state$raw_view <- isTRUE(input$raw_view)
        }, ignoreNULL = FALSE)
```

If the surrounding observer uses `br()$state$...` (i.e. `br` is a reactive function rather than a list), match that exactly. Use the existing observer as a template — copy its assignment style.

- [ ] **Step 3: Smoke test that the package still loads**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::load_all(); message("server compiles OK")'
```

Expected: no errors.

- [ ] **Step 4: Run all tests**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test()'
```

Expected: full suite PASS (skips for missing chrome/db are fine).

- [ ] **Step 5: Commit**

```bash
cd /home/aviezerl/src/misha.browser && git add R/shiny-server.R && git commit -m "Shiny server: observer wires raw_view checkbox to state"
```

---

## Task 9: NEWS / README

**Files:**
- Modify: `NEWS.md` (if present), `README.Rmd` (only if it documents panel options)

- [ ] **Step 1: Check what exists**

```bash
cd /home/aviezerl/src/misha.browser && ls NEWS.md NEWS 2>/dev/null; grep -n "show_name\|^.*raw " README.Rmd 2>/dev/null | head -5
```

- [ ] **Step 2: Update NEWS.md if it exists**

If `NEWS.md` exists, prepend (under the topmost "development" section, or create one):

```markdown
# misha.browser (development)

* `browser_add_panel()` gains `show_name` (default `TRUE`) which renders the panel name as a left-side strip label, and `raw` (default `NULL`, inheriting from `cfg$plot$raw`) which renders the panel without smoothing and surfaces NAs as gaps.
* Shiny UI gains a "Raw" checkbox next to the smooth slider that toggles raw view session-wide.
```

If `NEWS.md` does not exist, skip this sub-step.

- [ ] **Step 3: README**

If `README.Rmd` has a section listing panel options or arguments, add `show_name` and `raw` there. Otherwise skip — the man page already documents both.

If you edit `README.Rmd`, re-knit:

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'rmarkdown::render("README.Rmd")'
```

- [ ] **Step 4: Commit (if anything changed)**

```bash
cd /home/aviezerl/src/misha.browser && git add NEWS.md README.Rmd README.md 2>/dev/null && git commit -m "Document show_name and raw view in NEWS/README" || echo "No README/NEWS changes"
```

---

## Task 10: Final integration verification

- [ ] **Step 1: R CMD check**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::check(args = c("--no-manual"), error_on = "warning")'
```

Expected: 0 errors, 0 warnings. NOTEs about new arguments are acceptable.

- [ ] **Step 2: Full test suite**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e 'devtools::test()'
```

Expected: all tests PASS (skips for missing chrome/db are fine).

- [ ] **Step 3: API smoke test**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e '
devtools::load_all()
br <- browser_create() |>
  browser_add_panel(name = "smoothed", tracks = "t1") |>
  browser_add_panel(name = "raw_pinned", tracks = "t1", raw = TRUE) |>
  browser_add_panel(name = "no_strip", tracks = "t1", show_name = FALSE)
print(br$cfg$panels[[1]]$show_name)   # TRUE
print(br$cfg$panels[[1]]$raw)         # NULL (inherit)
print(br$cfg$panels[[2]]$raw)         # TRUE
print(br$cfg$panels[[3]]$show_name)   # FALSE
'
```

Expected output (in order): `TRUE`, `NULL`, `TRUE`, `FALSE`.

- [ ] **Step 4: Render a real plot if a misha db is available**

```bash
cd /home/aviezerl/src/misha.browser && Rscript -e '
devtools::load_all()
if (!file.exists("dev/test_config.yaml")) {
  message("No test config — skipping")
  quit()
}
br <- browser_create(config = "dev/test_config.yaml")
# Toggle raw on first panel and re-plot
br$cfg$panels[[1]]$raw <- TRUE
p <- browser_plot(br)
ggplot2::ggsave("/tmp/raw_plot_check.png", p, width = 10, height = 8, dpi = 100)
cat("Saved /tmp/raw_plot_check.png\n")
'
```

Open `/tmp/raw_plot_check.png` and confirm: panel name strips visible on the left, first panel renders raw (no smooth curve, gaps where data is missing).

---

## Out of Scope (per spec)

- UCSC-style barplot (deferred).
- Changing `rollmean` fill behavior for non-raw renders.
- Per-bp raw resolution.
