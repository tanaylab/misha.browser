% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{browser_plot}
\alias{browser_plot}
\title{Plot the genome browser}
\usage{
browser_plot(
  browser,
  region = NULL,
  gene = NULL,
  span = NULL,
  parallel = getOption("misha.browser.parallel", FALSE),
  profile = getOption("misha.browser.profile", FALSE)
)
}
\arguments{
\item{browser}{Browser object}

\item{region}{Optional region to plot (uses current if NULL)}

\item{gene}{Optional gene name to center on}

\item{span}{Optional span in bp (used with gene)}

\item{parallel}{Logical, enable parallel panel data extraction (default: from option).
Requires \code{future} package and a parallel plan to be set
(e.g., \code{future::plan(future::multisession)}).}

\item{profile}{Logical, enable profiling output (default: from option)}
}
\value{
A patchwork plot object
}
\description{
Renders all panels for the current or specified region.
}
\examples{
\dontrun{
browser <- browser_create(config = "config.yaml")
browser_plot(browser)
browser_plot(browser, gene = "Tbx5", span = 2e6)

# Enable profiling
options(misha.browser.profile = TRUE)
browser_plot(browser)

# Enable parallel extraction
future::plan(future::multisession, workers = 4)
browser_plot(browser, parallel = TRUE)
}
}
