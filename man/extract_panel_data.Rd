% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract_panel_data}
\alias{extract_panel_data}
\title{Extract data for a panel}
\usage{
extract_panel_data(browser, panel, region, use_cache = TRUE)
}
\arguments{
\item{browser}{Browser object}

\item{panel}{Panel configuration}

\item{region}{Interval to extract (data frame with chrom, start, end)}

\item{use_cache}{Whether to use caching}
}
\value{
Data frame with extracted and transformed data
}
\description{
Extracts track data for a panel at the given region, applies transforms.
}
\details{
Supports three extraction modes configured via \code{plot.extraction_mode}:
\itemize{
\item "fixed" (default): Uses fixed iterator + rollmean smoothing. Best for
vtracks with \code{func=sum} where values scale with bin size.
\item "dynamic": Adjusts iterator based on view span (like misha.vis). The
iterator itself provides smoothing via aggregation, so rollmean is skipped.
More efficient for large regions but values may differ at different zoom levels.
\item "dynamic_smooth": Adjusts iterator for resolution AND dynamically sets
vtrack sshift/eshift for smoothing. Combines adaptive resolution with
proper value scaling. Uses \code{smoothing_bp} config or state for window size.
}
}
\keyword{internal}
