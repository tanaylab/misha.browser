# Move view right

Move view right

## Usage

``` r
browser_move_right(browser, fraction = .DEFAULT_MOVE_FRACTION)
```

## Arguments

- browser:

  Browser object

- fraction:

  Fraction of view width to move (default 0.5)

## Value

Updated browser object
