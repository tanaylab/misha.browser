# Config Editor Module Server

Config Editor Module Server

## Usage

``` r
config_editor_server(id, browser_rv, original_config)
```

## Arguments

- id:

  Module namespace ID

- browser_rv:

  Reactive value containing browser object

- original_config:

  Original configuration for reset

## Value

NULL (module server)
