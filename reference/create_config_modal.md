# Create the config editor modal

Create the config editor modal

## Usage

``` r
create_config_modal(ns, cfg)
```

## Arguments

- ns:

  Namespace function

- cfg:

  Current configuration

## Value

Modal dialog
