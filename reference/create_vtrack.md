# Create a virtual track from configuration

Create a virtual track from configuration

## Usage

``` r
create_vtrack(vt, cfg = NULL)
```

## Arguments

- vt:

  Vtrack configuration

- cfg:

  Browser configuration (for resolving paths)
