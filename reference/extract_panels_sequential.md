# Extract data for multiple panels sequentially

Sequential fallback for parallel extraction.

## Usage

``` r
extract_panels_sequential(browser, panels, region, use_cache = TRUE)
```

## Arguments

- browser:

  Browser object

- panels:

  List of panel configurations

- region:

  Viewing region

- use_cache:

  Whether to use caching

## Value

Named list of extracted data frames
