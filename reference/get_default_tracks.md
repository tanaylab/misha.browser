# Get default selected tracks

Get default selected tracks

## Usage

``` r
get_default_tracks(browser)
```

## Arguments

- browser:

  Browser object

## Value

Character vector of default track names
