# Reset vtrack iterators to default

Removes custom sshift/eshift from virtual tracks by calling
gvtrack.iterator with NULL values.

## Usage

``` r
reset_vtrack_iterators(vtracks)
```

## Arguments

- vtracks:

  Character vector of vtrack names
