# Resolve a path relative to a base directory

Resolve a path relative to a base directory

## Usage

``` r
resolve_path(base_dir, path)
```

## Arguments

- base_dir:

  Base directory

- path:

  Path to resolve (absolute paths returned as-is)

## Value

Resolved absolute path
