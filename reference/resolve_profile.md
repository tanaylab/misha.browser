# Resolve profile and paths

Resolve profile and paths

## Usage

``` r
resolve_profile(cfg, profile = NULL, config_dir = ".")
```

## Arguments

- cfg:

  Configuration list

- profile:

  Profile name (NULL for auto-detect)

- config_dir:

  Directory containing config file

## Value

Configuration with resolved profile
