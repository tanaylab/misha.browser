# Smooth transform (rolling mean)

Smooth transform (rolling mean)

## Usage

``` r
transform_smooth(x, params)
```

## Arguments

- x:

  Numeric vector

- params:

  List with: window (required), align ("center", "left", "right")

## Value

Smoothed vector
